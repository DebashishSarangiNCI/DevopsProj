"""
accounts/models.py: This module contains the definitions for the data models used in the application.

This module includes the definitions for the data models used in the application,
which are created using Django's built-in `models` module. These models represent
the structure of the database tables used to store data in the application, and
define the fields, relationships, and behavior of the data objects used in the application.
"""

from django.db import models # noqa

# Create your models here.
